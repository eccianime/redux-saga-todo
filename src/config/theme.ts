import { extendTheme } from 'native-base';

export default extendTheme({
  fonts: {
    medium: 'Ubanist-Medium',
    bold: 'Ubanist-Bold',
  },
  sizes: {
    50: 200,
    15: 60,
    18: 72,
  },
});
